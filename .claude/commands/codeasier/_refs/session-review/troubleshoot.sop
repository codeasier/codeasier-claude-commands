# Session Review Troubleshoot Reference

在进入本模式前，先遵循 `shared.sop` 中的通用角色、原则、定位、时间线和证据提取要求。

## 模式目标

围绕“为什么失败 / 卡住 / 没做完”展开分析，定位主阻塞点，给出严重级别，并输出最小可恢复动作。
如果提供了 `focus`，则额外回答：该偏好所指向的疑问，是否就是主阻塞点，还是只是表象。

## 专项分析 SOP

### 第 1 步：识别当前阶段

至少判断：
- 会话停在什么阶段
- 最后一个有效推进动作是什么
- 最后一个异常动作是什么
- 若存在 `focus`，它主要对应哪个阶段或哪个异常片段

### 第 2 步：归因问题

判断问题属于以下哪类，可多选，但必须区分主因与次因：
- 工具报错
- 权限被拒
- 参数不合法
- 路径错误
- 上下文漂移
- 重复无效尝试
- 用户中断
- 信息不足导致无法继续

同时判断：
- `focus` 指向的是主因、次因，还是无直接证据支持的怀疑点

### 第 3 步：审查工具调用质量

重点判断：
- 哪些工具成功推进了工作
- 哪些工具调用失败或无效
- 是否出现重复尝试同一失败动作但未调整策略
- 是否本应换工具、换路径或补证据，但没有调整
- 若围绕 `focus` 继续分析，还缺哪类工具证据

### 第 4 步：恢复评估

输出恢复建议时，必须给出：
- 下一步最合理动作
- 建议先补充的信息
- 恢复成本：低 / 中 / 高
- 若继续处理，最小可行动作
- 如果要优先回答 `focus`，最小补证动作是什么

## 严重级别分级

必须给出严重级别：

- **P0**：核心输入缺失或会话文件不可用，导致无法分析
- **P1**：主流程被明确阻断，例如权限拒绝、关键工具报错、关键参数错误、关键路径错误
- **P2**：主流程可继续但效率明显下降，例如重复无效调用、策略未调整、上下文偏移
- **P3**：存在瑕疵但不影响主流程判断，例如输出不完整、结构一般、补充信息不足

分级规则：
- 先判断最高优先级主问题
- 再列出次级问题
- 不要把所有问题都抬成高优先级

## 输出格式

严格使用以下结构：

### Executive Summary
- 会话 ID
- 会话文件路径
- 分析偏好（若未提供可写“未指定”）
- 当前状态
- 严重级别（P0/P1/P2/P3）
- 一句话结论

### Timeline
- 阶段 1：
- 阶段 2：
- 阶段 3：

### Evidence Table
| 类型 | 证据 | 说明 |
|---|---|---|
| 目标证据 |  |  |
| 执行证据 |  |  |
| 异常证据 |  |  |
| 终止证据 |  |  |
| 偏好相关证据 | 若未提供可写“未指定” |  |

### Root Cause Analysis
- 主因：
- 次因：
- 这是事实还是推断：
- 判断依据：
- 分析偏好与主因的关系：若未提供可写“未指定”

### Tooling Review
- 成功推进的工具调用：
- 失败 / 被拒绝 / 无效的工具调用：
- 是否存在重复失败但未调整策略：

### Recovery Playbook
- 建议下一步：
- 建议先补充的信息：
- 恢复成本：低 / 中 / 高
- 若继续处理，最小可行动作：
- 若优先回答分析偏好，最小补证动作：若未提供可写“未指定”

### Risks / Unknowns
- 当前仍不确定的点：
- 若判断错误，最可能影响哪里：
